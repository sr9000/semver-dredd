# Agent Notes — Legacy `semver-dredd-java/`

Legacy/compatibility remnant, NOT a complete modern plugin package.

- Lacks the package metadata that `java-1.8-dredd/` has.
- Do NOT treat it as the reference Java plugin.
- Use `plugins/java-1.8-dredd/` for regex Java work.
- Use `plugins/javaparser-1.8-dredd/` for AST Java work.
- Do NOT implement include/exclude here unless the owner confirms this directory
  is still shipped/used.
