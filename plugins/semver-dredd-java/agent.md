# Agent Notes — Legacy `semver-dredd-java/`

This directory appears to be a legacy or compatibility remnant, not a complete
modern plugin package.

## Caution

- It does not have the same visible package metadata as `java-1.8-dredd/`.
- Do not use it as the reference Java plugin implementation.
- Prefer `plugins/java-1.8-dredd/` for regex Java plugin work.
- Prefer `plugins/javaparser-1.8-dredd/` for AST Java parser work.

## Scope-related notes

Do not implement include/exclude behavior here unless the project owner confirms
this legacy directory is still shipped or used.
